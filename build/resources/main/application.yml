spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://172.17.0.1:5000/bithumb
    username: root
    password: root
    sql-script-encoding: UTF-8

  profiles:
    active: dev
  application:
    name: "board-service"

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    #    generate-ddl: true
    #    defer-datasource-initialization: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8080
#  servlet:
#    encoding:
#      charset: UTF-8
#      enabled: true
#      force: true
#  tomcat:
#    uri-encoding: UTF-8



